{
  "title": "nbtools",
  "description": "Settings for nbtools",
  "type": "object",
  "jupyter.lab.shortcuts": [
    {
      "command": "nbtools:insert-tool",
      "keys": [
        "G"
      ],
      "selector": ".jp-Notebook:focus"
    }
  ],
  "additionalProperties": false,
  "properties": {
    "nbtools": {
      "type": "boolean",
      "title": "List of packages to load",
      "description": "List of packages containing tools to load on kernel start",
      "default": true
    },
    "autoLoad": {
      "type": "array",
      "title": "List of packages to load",
      "description": "List of packages containing tools to load on kernel start",
      "default": ["nbtools"]
    }
  }
}